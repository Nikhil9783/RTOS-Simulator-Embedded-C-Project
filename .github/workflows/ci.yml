name: Build and Test RTOS Simulator

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
    - uses: actions/checkout@v4
    
    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: sudo apt-get install -y make gcc
    
    - name: Install dependencies (Windows)
      if: runner.os == 'Windows'
      run: |
        choco install make
        choco install mingw
    
    - name: Build
      run: make

    - name: Unit Tests
      run: make test

    - name: Smoke Test (Linux)
      if: runner.os == 'Linux'
      run: |
        timeout 3s ./rtos < /dev/null || test $? -eq 124
